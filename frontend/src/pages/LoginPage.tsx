import React from 'react';

/**
 * A React component that renders a page with a heading "Welcome to the Login Page".
 * This page is used as a placeholder for the login page.
 */
const LoginPage: React.FC = () => {
    return (
        <div>
            <h1>Welcome to the Login Page</h1>
        </div>
    );
};

export default LoginPage;